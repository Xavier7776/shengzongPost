import OnlyUsShell from '@/components/onlyus/shell/OnlyUsShell'

export default function OnlyUsMainLayout({ children }: { children: React.ReactNode }) {
  return <OnlyUsShell>{children}</OnlyUsShell>
}
