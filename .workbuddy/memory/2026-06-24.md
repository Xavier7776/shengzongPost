# 2026-06-24 Skills 爬虫 + 自动任务

## 完成事项
- **Skills 爬虫运行**：`npx tsx scripts/crawl-skills.ts` 成功运行，GitHub token 更新后从 GitHub 爬取 9 条 AI Agent Skills
  - 8 条成功写入 Neon skills 表，1 条因 slug 重复（`skills` 被 MiniMax-AI/skills 和 vuejs-ai/skills 冲突）被跳过
  - 数据库当前共 10 条记录（含之前 5/31 的 2 条旧数据）
  - 表字段：id, name, slug, description, content, source_url, source_type, stars, tags, category, cover_image, created_at, updated_at, chinese_summary
- **安装 tsx**：`npm install -D tsx`，`scripts/crawl-skills.ts` 正常运行
- **自动任务**：创建 automation `Skills 爬虫 - 每3天自动更新`（FREQ=DAILY;INTERVAL=3;BYHOUR=10），每天 10:00 自动运行

- **Trending 爬虫运行**：`npx tsx scripts/crawl-trending.ts` 成功运行
  - daily 30 条、weekly 30 条、growth 30 条，全部写入 `github_trending` 表
  - 今日共 90 条新记录
- **自动任务**：`Trending 爬虫 - 每3天自动更新`（FREQ=DAILY;INTERVAL=3;BYHOUR=10;BYMINUTE=30），10:30 自动运行

## 发现的问题
- slug 去重逻辑需改进：两个不同仓库名生成相同 slug（如 `skills`）时第二条写入报 duplicate key
- Dev server (port 3001) 未运行，API 验证待 server 启动后做
- skills 和 trending 两个爬虫都在 10:00~10:30 执行，GitHub API rate limit（已认证 5000/h）共用，可能冲突；建议 trending 延后到 10:30
