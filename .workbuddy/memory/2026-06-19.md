# 2026-06-19 工作日志

## Vercel 构建 ESLint 警告修复
- **settings/page.tsx:198** — `<img>` 加 eslint-disable
- **timeline/page.tsx:36,160,204** — 3 处 `<img>` 加 eslint-disable
- **movies/page.tsx:194,367** — 2 处 `<img>` 加 eslint-disable
- **drawing/page.tsx:54,79** — 2 处 useEffect 缺少 `game` 依赖，加 eslint-disable（故意用粒度字段避免对象引用变化导致的无限循环）

## 之前修复（已提交）
- HeartLineConnect.tsx / SpriteUpload.tsx / SideNav.tsx / TrendingCard.tsx — `<img>` eslint-disable
- PingButton.tsx — useEffect 补 burstParticles 依赖
