# 2026-06-19 工作日志

## Vercel 构建 ESLint 多轮修复
所有 `<img>` 和 `useEffect` 依赖 warnings + `react/no-unescaped-entities` errors 均已修复。

**关键发现**：PetImporter.tsx 的 `eslint-disable-next-line` 误用在同一行（应用 `eslint-disable-line`），且 `react/no-unescaped-entities` 是 **error** 级别会直接阻塞构建。
